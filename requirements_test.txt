# pytest pinned to 8.x: pytest 9 makes "sync test requesting async
# fixture" a hard error, and pytest-homeassistant-custom-component's
# documented autouse'd `enable_custom_integrations` fixture is async.
# Re-evaluate when that plugin ships pytest-9 support.
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-homeassistant-custom-component>=0.13
aioresponses>=0.7.6
freezegun>=1.5
# aiodns 3.5+ references `pycares.ares_query_*_result` attributes at
# module-load time (in DNSResolver type annotations). Those attributes
# only exist in pycares 4.10+, but pytest-homeassistant-custom-component's
# transitive deps cap pycares below that, so pip resolves to an
# incompatible pair and pytest fails at plugin-load. Cap aiodns
# below the version that introduced the breakage; pycares can be
# anything compatible with that aiodns.
aiodns<3.5
